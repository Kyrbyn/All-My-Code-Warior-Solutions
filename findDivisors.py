# Skipped def divisors(integer):
# Skipped     divisors = []
# Skipped     for i in range(1, integer + 1):
# Skipped         if i != 1 & i != integer:
# Skipped             print("Current i = " + str(i))
# Skipped             if integer % i == 0:
# Skipped                 print ("Divisor appended")
# Skipped                 print(divisors)
# Skipped                 divisors.append(i)    
# Skipped     str(divisors)        
# Skipped 
# Skipped liste = (divisors(50))
# Skipped print(liste)
