def expanded_form(num):
    num = list(map(int, str(num)))
    listLength = len(num)
    for length in listLength:
        number = 10*10
    print(listLength)
    print(num)
    print(number)

expanded_form(123)